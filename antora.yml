name: desktop
title: Desktop App
version: '6.0'
start_page: ROOT:index.adoc
nav:
- modules/ROOT/partials/nav.adoc

asciidoc:
  attributes:
    latest-desktop-version: {page-component-version}   # do not change, this is the value of the version key
    previous-desktop-version: {page-component-version} # do not change, this is the value of the version key
    #
    # the following is a key that is used for conditional rendering
    # currently allowed values are: 'oc' and 'kw' 
    targetbuild: oc
    #
    # kw build version
    kw_version: 2.0
    #
    # this is the version string that is used for kw or oc to reference the binary
    kw_client-version: 2.0.0
    oc_client-version: 6.0.0
